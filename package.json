{
	"name": "nanostore-ipc-bridge-workspace",
	"private": true,
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"scripts": {
		"build": "npm run -ws build",
		"dev": "npm run -w @demo/test-electron dev"
	}
}
